{
    "compilerOptions": {
        // Node.js를 평범하게 사용, 그 외 여러가지를 import, export 가능해짐
        "module": "commonjs",
        // JavaScript Compile version
        "target": "ES2015",
        // SourcemMap 처리를 하고 싶은지 설정하는 부분
        // 소스 맵(Source Map)이란 배포용으로 빌드한 파일과 원본 파일을 서로 연결시켜주는 기능
        "sourceMap": true,
        "outDir": "dist"
    },
    // 컴파일 과정에 필요한 파일들 목록. 여러가지 있으면 여러가지를 배열 형태로 적으면 됨
    "include": ["src/**/*"],
    // Node modules를 설치하지 않아도 디폴트로 제외해놓는 것이 좋음
    "exclude": ["node_modules"]
}
